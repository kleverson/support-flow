spring:
  application:
    name: SupportFlow
  datasource:
    url: ${DB_URL:jdbc:postgresql://ep-tiny-union-ai63np7w-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require}
    username: ${DB_USERNAME:neondb_owner}
    password: ${DB_PASSWORD:npg_ukJa8rBh3GOD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: ${JPA_SHOW_SQL:false}
    open-in-view: false

app:
  jwt:
    secret: ${JWT_SECRET:change-me-change-me-change-me-change-me}
    access-exp-seconds: ${JWT_ACCESS_EXP_SECONDS:900}
    refresh-exp-seconds: ${JWT_REFRESH_EXP_SECONDS:2592000}
